# Server-side only: set this in Vercel, never prefix it with VITE_.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Optional public API origin for a desktop build. Leave empty for the web app.
VITE_API_BASE_URL=https://mon-projet-mu-two.vercel.app
