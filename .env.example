# Public Neon Auth endpoint. This URL is safe to expose to the browser.
VITE_NEON_AUTH_URL=https://your-project.neonauth.region.aws.neon.build/your_database/auth

# Server-side only: set this in Vercel, never prefix it with VITE_.
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
